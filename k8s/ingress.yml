---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-as-ingress
  namespace: gateway-ingress-v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway-as-ingress
  template:
    metadata:
      labels:
        app: gateway-as-ingress
    spec:
      serviceAccountName: service-account
      containers:
      - image: zhaodockerhub/gateway-as-ingress:v2.2
        imagePullPolicy: Always
        name: gateway-as-ingress
        ports:
        - containerPort: 8080
        env:
        - name: LOGGING_LEVEL_ROOT
          value: TRACE
        resources:
          limits:
            memory: "300Mi"
          requests:
            memory: "300Mi"
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
            scheme: HTTPS
          initialDelaySeconds: 15
          timeoutSeconds: 3
          periodSeconds: 10
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
            scheme: HTTPS
          initialDelaySeconds: 180
          timeoutSeconds: 3
          periodSeconds: 10
          failureThreshold: 3
        
---
kind: Service
apiVersion: v1
metadata:
  name: gateway-as-ingress
  namespace: gateway-ingress-v1
  labels:
    app: gateway-as-ingress
spec:
  selector:
    app: gateway-as-ingress
  ports:
  - protocol: TCP
    port: 8080
    name: http
